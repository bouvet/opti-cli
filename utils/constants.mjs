// import path from 'path';
// import { getProjectConfig } from '../helpers/project-config.mjs';

// export const constants = {
//   defaultDBPort: 1433,
// };

// make into process.process.opti.projectConfig
// export const env = {
//   projectConfig: getProjectConfig(),
//   projectName: path.basename(process.cwd()).toLowerCase(), // should maybe store this in project config
// };
